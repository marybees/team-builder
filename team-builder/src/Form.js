import React, { useState } from 'react';

const Form = props => {
    return (
        <form>
            <label htmlFor='title'></label>
            <input id = 'title' type='text'/>
        </form>
    )
};

export default Form